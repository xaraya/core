This directory contains directories with javascript libraries.
The required structure is:

common/lib
     |
     --- library name (directory)
                |
                --- library versions (directory)
                |          |
                |          --- library file
                |
                --- plugins (directory)
                        |
                        --- plugin versions (directory)
                                |
                                --- plugin file