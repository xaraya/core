<?xml version="1.0" encoding="utf-8"?>
<xar:template xmlns:xar="http://xaraya.com/2004/blocklayout">
    <!-- License: GPL http://www.gnu.org/copyleft/gpl.html -->
    <xar:if condition="isset($value)">
        <xar:if condition="is_array($value)">
        <xar:if condition="!empty($object) and empty($singlevalue)">
            <xar:set name="values">[$source => $value]</xar:set>
            <xar:data-view object="$object" items="$values" fieldlist="$fieldlist" linktype="object" newlink=""/>
        <xar:else/>
        <div class="xar-form-container">
            <xar:if condition="!empty($link) and !empty($source)">
                <xar:if condition="str_contains($link, '[itemid]')">
                    <xar:set name="itemlink">str_replace('[itemid]', $source, $link)</xar:set>
                <xar:else/>
                    <xar:set name="itemlink">$link</xar:set>
                </xar:if>
                <div class="xar-row">
                    <div class="xar-col">
                        <label title="" style=""><a href="#$itemlink#">#$source#</a>:</label>&#160;
                    </div>
                </div>
            </xar:if>
            <xar:foreach in="$value" key="$key" value="$val">
                <div class="xar-row">
                    <div class="xar-col">
                    <label title="" style="">#$key#:</label>&#160;
                    <xar:var name="val"/>
                    </div>
                </div>
            </xar:foreach>
        </div>
        </xar:if>
        <xar:elseif condition="!empty($link)"/>
            <xar:if condition="str_contains($link, '[itemid]')">
                <xar:set name="itemlink">str_replace('[itemid]', $source, $link)</xar:set>
            <xar:else/>
                <xar:set name="itemlink">$link</xar:set>
            </xar:if>
            <a href="#$itemlink#">#$value#</a>
        <xar:else/>
            <xar:var name="value"/>
        </xar:if>
    </xar:if>
</xar:template>
